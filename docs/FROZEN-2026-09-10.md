# Accepted baseline — 2026-09-10

- Compact outlined header pills for Log Out, Restart and Shut Down.
- Optional Switch User entry in the overflow menu.
- Configurable visibility: logout/restart/shutdown enabled by default,
  switch-user disabled by default.
- Direct logout; restart and shutdown retain system confirmation.
- System Settings moved to the System Tray header.
- Header tooltips positioned inside the window's safe edges.

The controls and tooltip correction were accepted in live testing. All four
tests pass. Existing notification and ticker behavior is unchanged by this batch.

## Accepted rail and navigation polish

- Bell keeps its optical baseline after notification dismissal; its counter
  sits lower within the panel bounds.
- Weather icon is centered independently of its lower temperature readout.
- Battery percentage comes from UPower first. Confirmed AC power with fully
  charged/pending-charge state displays a bolt; unknown state is not assumed AC.
- System Tray has a rounded upward triangle with a thin, uniformly offset
  active ripple following the same contour, rather than scaled vertices.
- Removed the host-level back buttons. Repeated activation of the current dock
  destination closes it; choosing a different destination switches views.

User accepted the final appearance and behavior. All four tests pass, including
AC-state and open/close/switch regression fixtures. Installed and tested plugin
SHA-256: `e49bda185d390f95bfb1d468d678874d50c4dba5cfc4af53c5116a338f90d088`.
Kadunce's live safety control was verified at freeze; no Kadunce or Tette source
changes are included. Local candidate installation now backs up the plugin and
reloads Plasma in one command, without restarting KWin or logging out.
