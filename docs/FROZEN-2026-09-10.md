# Accepted baseline — 2026-09-10

- Compact outlined header pills for Log Out, Restart and Shut Down.
- Optional Switch User entry in the overflow menu.
- Configurable visibility: logout/restart/shutdown enabled by default,
  switch-user disabled by default.
- Direct logout; restart and shutdown retain system confirmation.
- System Settings moved to the System Tray header.
- Header tooltips positioned inside the window's safe edges.

The controls and tooltip correction were accepted in live testing. All four
tests pass. Existing notification and ticker behavior is unchanged by this batch.
