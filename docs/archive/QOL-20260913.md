# Bluetooth, badge and notification readability freeze

Installed and accepted by J on September 13, 2026; authorized for publication.
Verified installed SHA-256:
`f284e06cb5c0f4fb84ece3036143501f49a86a40e1dab442162d99ad90edabc0`.
All five automated checks pass, including mouse/touch notification fixtures.

Follow-up: the first QoL binary was installed. Bluetooth pairing now uses the
established 30px rounded pill visual inside the unchanged 44px touch target,
including hover/pressed fill and keyboard-focus outline. J accepted this final polish.

- Bluetooth's native applet page gets Add new device in the shared header.
  It launches the installed org.kde.bluedevilwizard desktop service through
  the existing KIO application launcher. No custom pairing or device mutations.
- Bell counter grows from 8px (6pt at 96dpi) to 7pt. Visible text bounds sit
  one logical pixel below the painted bell rim, clamped inside the rail. The
  bell's centered geometry is unchanged; test small rails and 1–3 digit counts.
- Standalone and grouped history children share the same expandable delegate.
  Explicit StyledText makes formatted bodies obey the line limit and expose
  truncation; summaries are plain text. Show more expands the full child in place.
  Truncated critical banners offer Show more leading to notification history.
- Card background clicks/taps invoke the default notification action first;
  history falls back to the desktop entry when no default action exists. With
  neither, the history card is non-actionable. The rail opens review rather than
  executing a notification. Critical banners without a default action open review.
- Child buttons consume their own clicks; expanding history must not launch the
  notification. Automated fixtures cover default action, app fallback and expanded
  formatted text for standalone/grouped children. No live notifications are invoked.

Acceptance scope: native pairing dialog, badge alignment/counts, expanded
grouped long messages, notification actions, and the final Bluetooth pill polish.
Local installer archives preserve rollback; they are not shipped in the repository.
This does not implement the deferred stock-notification presenter handoff.
